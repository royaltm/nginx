server {
  listen   80;
  server_name  {{nginx_default_hostname}};

  access_log  {{nginx_log_dir}}/default.access.log;

  location / {
    root   {{nginx_default_root}};
    index  index.html index.htm;
  }
}
